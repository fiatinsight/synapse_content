// Makes an item unselected
$('.check-box.selected[data-content-label-id="<%= @content_label_assignment.content_label_id %>"]').bind('ajax:success', function() {
  $(this).replaceWith("<%= j render('add_content_label_assignment_link') %>");
  console.log("destroy.js invoked <%= @content_label_assignment.content_label_id %>")
});
